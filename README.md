# Implementation of Mo's Algorithm in Java

### OddSumLogicInterface

This logic interface will be utilized in Mo's Algorithm to find the Sum of integers in a list which has odd frequency with the given range.
